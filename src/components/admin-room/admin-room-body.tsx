"use client";

import { IAdminRoom } from "@/interfaces/room-info";
import { cn } from "@/lib/utils";

export default function AdminRoomBody({ room }: { room: IAdminRoom }) {
  return (
    <main className={cn("w-screen h-screen flex justify-center items-center gap-4 flex-row")}>
      
    </main>
  )
}
